/**
 * Logger Utility
 * Re-export shared logger for booking service
 * All services should use the shared structured logger
 */

export { default as logger } from '@kodingcaravan/shared/config/logger';

