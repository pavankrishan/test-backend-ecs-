# Notification Service – Environment Example
# Copy to kc-backend/.env or set in your environment (notification-service uses shared config from root).

# --- FCM (Firebase Cloud Messaging) - Push notifications ---
# Use ONE of the following options:

# Option A: Service account JSON file path (recommended for local/production)
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/your-firebase-service-account.json

# Option B: Service account from env (for Docker/CI – no file on disk)
# Get these from Firebase Console → Project Settings → Service accounts → Generate new private key
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Option C: Application Default Credentials (gcloud)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_SERVICE_ACCOUNT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
# Then run: gcloud auth application-default login

# Project ID must match your mobile app’s Firebase project (e.g. mapskc or kodingcaravan-c1a5f).
FIREBASE_PROJECT_ID=mapskc

# --- Service ---
NOTIFICATION_SERVICE_PORT=3006
NOTIFICATION_PORT=3006

# --- MongoDB (shared with backend) ---
# MONGO_URI=mongodb+srv://...
# MONGO_DB_NAME=kodingcaravan

# --- Optional: SMTP, MSG91, VAPID ---
# SMTP_HOST= SMTP_PORT= SMTP_USER= SMTP_PASS=
# MSG91_AUTH_KEY= MSG91_SENDER= MSG91_TEMPLATE_ID=
# VAPID_PUBLIC_KEY= VAPID_PRIVATE_KEY=
