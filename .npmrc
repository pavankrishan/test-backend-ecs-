# Skip lifecycle scripts so `pnpm install` completes on Windows (nx postinstall can fail with MODULE_NOT_FOUND).
# To run scripts anyway: pnpm install --no-ignore-scripts
ignore-scripts=true

# Flatten node_modules so pg/pg-protocol resolve correctly on Windows (avoids symlink resolution issues).
node-linker=hoisted
